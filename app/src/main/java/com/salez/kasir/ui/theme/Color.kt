package com.salez.kasir.ui.theme

import androidx.compose.ui.graphics.Color

// Warna utama
val PrimaryPeach = Color(0xFFE9A397)  // Warna primary (peach)
val PrimaryVariant = Color(0xFFD86B5B)  // Peach lebih gelap
val Secondary = Color(0xFF9F6BCC)  // Ungu untuk tombol aktif
val SecondaryVariant = Color(0xFF7F4AAA)  // Ungu lebih gelap

// Warna background
val BackgroundStart = Color(0xFFFED8D3)  // Gradient start (peach muda)
val BackgroundEnd = Color(0xFFBB92C5)    // Gradient end (ungu muda)

// Warna status
val PendingColor = Color(0xFF282828)  // Hitam untuk status menunggu
val PendingText = Color(0xFFFF7754)   // Teks orange untuk status menunggu
val ProcessingColor = Secondary       // Ungu untuk status diproses
val CompletedColor = Color(0xFF7EDAD1)  // Mint untuk status selesai

// Warna tambahan
val CardBackground = Color.White
val TextPrimary = Color(0xFF282828)    // Teks utama (hitam)
val TextSecondary = Color(0xFF757575)  // Teks sekunder (abu-abu)